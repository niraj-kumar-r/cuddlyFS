debug: true

namenode:
    bind_address: "[::1]:50051"
    name_dir: "/tmp/cuddlyfs/namenode"

datanode:
    namenode_rpc_address: "http://[::1]:50051"
    data_dir: "/tmp/cuddlyfs/datanode"
    disk_check_interval: 3000

# 64 * 1024 * 1024
block_size: 67108864
replication_fator: 3

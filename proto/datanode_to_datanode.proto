syntax = "proto3";

package cuddlyproto;

import "common.proto";
import "client_to_datanode.proto";

service DatanodeDataNodeService {
  rpc ReplicateDatanodeBlock (stream Packet) returns (WriteBlockResponse);
}